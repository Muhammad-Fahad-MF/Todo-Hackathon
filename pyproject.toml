[project]
name = "todo-hackathon"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "typer",
    "rich"
]

[project.scripts]
start = "todo.main:app"

[tool.setuptools.packages.find]
where = ["src"]

